const { getInput, getInputLines } = require('./input');
const {
  writeDebug,
  writeInfo,
  writeWarning,
  writeError,
  setOutput,
  fail,
} = require('./output');

module.exports = {
  getInput,
  getInputLines,
  writeDebug,
  writeInfo,
  writeWarning,
  writeError,
  setOutput,
  fail,
};
